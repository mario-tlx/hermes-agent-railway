# Web server
PORT=8080

# Optional: TOTP (Microsoft Authenticator, Google Authenticator). Base32 secret, e.g.:
# ADMIN_TOTP_SECRET=JBSWY3DPEHPK3PXP
# ADMIN_SESSION_SECRET=  # optional; openssl rand -hex 32

# All other Hermes configuration (LLM providers, messaging channels, tool API keys)
# is managed through the web UI at /
